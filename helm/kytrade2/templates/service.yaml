apiVersion: v1
kind: Service
metadata:
  name: {{ include "kytrade2.fullname" . }}
  labels:
    {{- include "kytrade2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kytrade2.selectorLabels" . | nindent 4 }}
